name: crypto-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - duckdb
  - pyarrow
  
  - pip:
    - gcsfs
    - google-cloud-storage
    - pandas
    - pytest
    - pytest-mock
    - python-dotenv
    - requests
    - urllib3