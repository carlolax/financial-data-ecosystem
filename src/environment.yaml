name: crypto-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - duckdb
  - pyarrow
  
  - pip:
    - fastparquet
    - gcsfs
    - google-cloud-storage
    - pandas
    - plotly
    - pytest
    - pytest-mock
    - python-dotenv
    - requests
    - streamlit
    - urllib3